[package]
name = "mypaint-rs"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
rand = "0.10.0"
serde_json = "1.0.149"

[build-dependencies]
serde_json = "1.0.149"   # build.rs 解析 brushsettings.json

[lib]
crate-type = ["cdylib", "staticlib"]

[features]
original = []